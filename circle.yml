machine:
  python:
    version: 3.5.0rc1
  services:
    - postgresql
  environment:
    FLASK_CONFIG: testing
test:
  override:
    - python manage.py test
database:
  override:
    - psql -c 'create database "test-database";' -U ubuntu
deployment:
  staging:
    branch: master
    heroku:
      appname: afternoon-gorge-4661
