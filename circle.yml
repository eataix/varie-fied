machine:
  python:
    version: 3.5.0
  services:
    - postgresql
  environment:
    FLASK_CONFIG: testing
dependencies:
  pre:
    - npm install
    - grunt
database:
  override:
    - psql -c 'create database "test-database";' -U ubuntu
