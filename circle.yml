machine:
  python:
    version: 3.5.0
  services:
    - postgresql
  environment:
    FLASK_CONFIG: testing
test:
  override:
    - python manage.py test
database:
  override:
    - psql -c 'create database "test-database";' -U ubuntu
